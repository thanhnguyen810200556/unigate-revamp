function Logo({ className }) {
  return (
    <div className="flex items-center">
      <h1 className={`text-xl ${className} font-semibold text-gray-800`}>
        VNUHCM UNIGATE
      </h1>
    </div>
  );
}
export default Logo;
